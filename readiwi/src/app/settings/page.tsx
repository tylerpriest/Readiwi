import SettingsPage from '@/plugins/settings/components/SettingsPage';

export default function Settings() {
  return (
    <div className="min-h-screen bg-background">
      <SettingsPage />
    </div>
  );
}